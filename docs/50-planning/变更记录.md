---
status: current
owner: Dev Team
last-reviewed: 2026-07-19
---

# 变更记录

## 2026-07-19

### 上游同步

- 合入 `upstream/main`（`d27b491e`），包含 7 个上游提交、40 个文件变更。
- 渠道能力新增 Codex 与 Advanced Custom 上游模型发现，补充配置 DTO、模型服务、API 文档和回归测试。
- 修复 Responses 转 Chat 流式转换中的重复工具调用。
- 用户列表改为服务端排序，分页数据不再由客户端局部排序；数据表列显隐后立即刷新表体。
- 统一 OpenAI Models 文案，并更新高风险状态码重试确认提示及七种前端语言。

### 组织 Invoice 与结算系数

- 完成 `9235ad8d`：新增组织 Invoice 聚合、北京时间账期、模型类别 × 账号和完整模型 × 账号两张交叉表。
- 新增组织月度结算规则表、四位定点系数、默认/继承/多档规则、版本 CAS、失败审计和负消费保护。
- 新增当前组织与系统管理员的 Invoice 查询、规则查询/更新和专用 CSV；Member 由服务端拒绝访问完整 Invoice。
- 管理员组织详情增加 Invoice Tab，组织 Admin 增加独立 Invoice 页面、规则配置 Sheet、本月/上月/自定义账期和七种语言文案。
- Billing 与 Logs 明细导出改为有界批次流式输出，不再固定截断 10,000 行。
- SQLite model/controller 自动化测试、前端 typecheck/lint/build 与 i18n 同步通过；MySQL、PostgreSQL、ClickHouse 实际环境验证和生产规模压测仍是发布门槛。

### 文档收口

- 将历史账单归属起点与 Invoice 开发草稿拆分沉淀到产品、数据模型、架构、工程、运维、UI 和规划文档。
- 补充组织账单组件图、历史归属预览/应用时序图，以及生产回填、错误处置和回滚手册。
- 移除临时开发目录及其全部入口和链接；后续一次性开发草稿不进入 `docs/`，稳定结论直接更新对应主题目录。

## 2026-07-15

- 组织管理与组织账单产品完成并上线。
- 生产范围统一为 Admin、Member 两类组织角色，并保持个人账号为唯一扣费主体。
- 组织账单已交付汇总、成员、模型、渠道、趋势、明细和导出能力。
- 管理员侧和用户侧组织页面已完成接入，Member 账单范围由服务端强制限制为本人。
- 产品、架构、UI、发布说明和规划文档已同步为生产状态。
